public abstract class Item {
    private String name;

    // Konstruktor
    public Item(String name) {
        this.name = name;
    }

    // Getter
    public String getName() {
        return name;
    }

    
    public abstract void use();
}

