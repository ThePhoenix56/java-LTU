public class Weapon extends Item {
    private int damage;

//Skappar ett vapen som tar skada 

    public Weapon(String name, int damage) {
        super(name);
        this.damage = damage;
    }

    @Override
    public void use() {
        System.out.println("Vapnet används och gör " + damage + " skada.");
    }

    public int getDamage() {
        return damage;
    }
}
